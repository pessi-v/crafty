{% extends "_layout" %}

{% block content %}
<div id="recipe-submit-container" class="flex-column">
  <h class="capitalize text-4xl">Submit a Recipe</h>

  {% if craft.app.session.hasFlash('notice') %}
    <div class="notice">
      {{ craft.app.session.getFlash('notice') }}
    </div>
  {% endif %}

  {% if craft.app.session.hasFlash('error') %}
    <div class="error">
      {{ craft.app.session.getFlash('error') }}
    </div>
  {% endif %}

  <form method="post" enctype="multipart/form-data" id="recipe-form" autocomplete="off">
    {{ csrfInput() }}
    {{ actionInput('recipesmodule/recipes/submit') }}

    <div class="flex-column">
      <div class="form-group">
        <label for="title" class="form-label">
          Title
        </label>
        <input
          type="text"
          id="title"
          name="title"
          required
          autocomplete="off"
          class="form-input"
        >
      </div>

      <div class="form-group">
        <label class="form-label">Categories</label>
        {% set allCategories = craft.categories().group('labels').all() %}
        <div class="categories-container">
          {% for category in allCategories %}
            <label class="category-checkbox-label">
              <input
                type="checkbox"
                name="categories[]"
                value="{{ category.id }}"
              >
              {{ category.title }}
            </label>
          {% endfor %}
        </div>
      </div>

      <div class="form-group">
        <label class="form-label">Recipe Content</label>
        <input type="hidden" name="contentBlocks[0][text]" value="text">
        <textarea
          name="contentBlocks[0][text]"
          rows="4"
          class="form-input"
        ></textarea>
      </div>

      <div class="form-group">
        <label for="salasana" class="form-label">
          Password
        </label>
        <input
          type="salasana"
          id="salasana"
          name="salasana"
          required
          autocomplete="off"
          class="form-input"
        >

      </div>

      <button type="submit" class="submit-button">
        Submit
      </button>
      <p class="form-note">
        <em>Your recipe will be reviewed before it appears on the site.</em>
      </p>
    </div>
  </form>
</div>

{% endblock %}
