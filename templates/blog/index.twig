{% extends '_layout' %}

{% set posts = craft.entries()
	.section('blog')
	.with([
    ['featureImage', { withTransforms: ['thumbnail'] }],
  ])
	.eagerly()
	.all() %}

{% block content %}
	<h1>Blog</h1>

	{% include 'blog/_feed' %}

{% endblock %}
