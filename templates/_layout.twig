<!DOCTYPE html>
<html lang="{{ craft.app.language }}">
	<head>
		<meta charset="utf-8"/>
		<title>{{ siteName }}</title>
		<meta content="width=device-width, initial-scale=1.0" name="viewport">

		<script type="importmap">
			{
				"imports": {
					"three": "https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.module.js",
					"three/addons/": "https://cdn.jsdelivr.net/npm/three@0.160.0/examples/jsm/"
				}
			}
		</script>

		{% do craft.app.view.registerCssFile('@web/styles.css') %}
		{% do craft.app.view.registerCssFile('@web/animation.css') %}
		{% do craft.app.view.registerCssFile('@web/modal.css') %}

	</head>
	<body>
		{# Get the first segment of the current URI: #}
		{# {% set navSegment = craft.app.request.getSegment(1) %} #}
		<header>
		</header>
		<main>
			{% block content %}
				{# Nothing here, yet! #}
			{% endblock %}
		</main>
		<footer>
			{# {{ radio.render }}	 #}
			{% include '_radio.twig' %}
			{# <div class="description">
				{{ siteInfo.description | md }}
			</div>
			<div class="copyright">&copy;{{ now | date('Y') }}
				{{ siteName }}</div>
			<div class="colophon">Built with
				<a href="https://craftcms.com/" target="_blank">Craft CMS</a>
			</div> #}
		</footer>
	</body>
</html>
